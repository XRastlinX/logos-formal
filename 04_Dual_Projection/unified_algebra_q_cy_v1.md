# UNIFIED ALGEBRA {q}/{cy} V1

**Record Type:** Formal Structural Algebra / Dual Projection Morphism
**Layer:** Atomic Governance Invariants
**Status:** ACTIVE_CANON
**Governing Rule:** Constitutio Alchemica

---

This artifact canonizes the `{q}/{cy}` dual projection, structurally bridging the Q-taxonomy (Cord content types) and the Cy-taxonomy (Governance geometry). It serves as the definitive structural link mapping computational primes, physical validation, and systemic governance into a single algebraically bound tensor.

## I. The Dual Projection

The operator `{q, cy}` acts upon a Cord state $(i, r, f)$ to extract both its **content identity** and its **governance location**:

\[
\{q\} : \text{Cord} \rightarrow \text{Content}
\]
\[
\{cy\} : \text{Cord} \rightarrow \text{Geometry}
\]

Jointly forming the dual morphism:
\[
\{q, cy\} : (i,r,f) \rightarrow (\text{Q-type},\ \text{Cy-state})
\]

## II. The Q-Taxonomy (Content)

The seven `q` terms define the Cord based exclusively on its interior ($i$) and residual ($r$) weights:

| Q-term | Cord interior ($i$) | Cord residual ($r$) | Meaning |
|---|---|---|---|
| **qbit** | $0$ or $1$ | $0$ | discrete interior |
| **quillia** | $\nu$ | $0$ | interpretive interior |
| **quanta** | $\nu$ | $1$ | structural residual |
| **qualon** | $\nu$ | $1$ | interior+residual hybrid |
| **qev** | $0$ | $1$ | collapse residual |
| **qemic** | $0$ | $0$ | null-prime unit |
| **qflux** | $\Delta(i,r)$ | $\Delta(i,r)$ | transition unit |

## III. The Cy-Taxonomy (Geometry)

The seven `cy` terms locate the Cord within the Cubed Bit state space:

| Cy-term | Cubed Bit | Meaning |
|---|---|---|
| **$cy_0$** | $000$ | sorted / pure witness |
| **$cy_\nu$** | $0\nu0$ | interpretive drift |
| **$cy_1$** | $101$ | authorized effect |
| **$cy_\Delta$** | transition | $0\nu0 \to 101$ motion |
| **$cy^0$** | collapse | $9:1 = 0^x$ |
| **$cy_\emptyset$** | null-prime | gap = 1 identity |
| **$cy_X$** | crossing | relational operator |

## IV. The Canonical Fusion Table

The following table formally maps Content (`q`) to Geometry (`cy`) and explicitly bridges them back to the Prime-Root field input structures.

| Q-term | Cy-term | Cord state | Prime-Root Origin |
|---|---|---|---|
| **qbit** | $cy_0$ or $cy_1$ | $(i=0/1, r=0)$ | A=2 (Small gap identity) |
| **quillia** | $cy_\nu$ | $(i=\nu, r=0)$ | A=3 (Interpretive drift) |
| **quanta** | $cy_1$ | $(i=\nu, r=1)$ | A=5 (Structural authorization) |
| **qualon** | $cy_\Delta$ | $(i=\nu, r=1)$ | A=7 (Hybrid motion) |
| **qev** | $cy^0$ | $(i=0, r=1)$ | C-root (Collapse state) |
| **qemic** | $cy_\emptyset$ | $(i=0, r=0)$ | gap = 1 (Null-prime) |
| **qflux** | $cy_X$ | $\Delta(i,r)$ | Gap transitions |

## V. Hard Invariants

The Algebra binds two unalterable invariants:

1. **The Null-Prime Identity:** 
   The singular gap ($3 - 2 = 1$) possesses no prime factorization. It strictly maps to the dual projection:
   \[ gap=1 \mapsto (\text{qemic},\ cy_\emptyset) \]
   It can never possess an A, B, C, or D root.

2. **The 010 Isolation Rule:** 
   The `010` (Interpretation-only) governance constraint structurally isolates `quillia`. A state marked `010` projects strictly to $cy_\nu$ and is **permanently forbidden** from leaking into or transitioning directly to $cy_1$ (Authorized effect) without passing through an external permit mechanism ($\alpha$).
