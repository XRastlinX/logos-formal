# Plug-and-Play Zip Architecture V0.1

**Record Type:** Epistemic Canon  
**Layer:** Service Surface  
**Status:** ACTIVE_CANON  

## 1. Dual Pathway Launchers

The `logos-formal` service surface is designed for immediate, decoupled execution without requiring complex environment bootstrapping. It is distributed as a lightweight, plug-and-play archive supporting dual operational pathways.

### Pathway A: Codex Oracle Mode
*   **Target:** External LLM interpreters, NotebookLM, or AI Studio.
*   **Mechanism:** The AI agent ingest the zip bundle, parsing the repository context to provide read-only, theoretical exploration (`010` observer).
*   **Boundary:** strictly enforced by the `ORACLE_SYSTEM_INSTRUCTION_V0_1`.

### Pathway B: Antigravity Local Execution Mode
*   **Target:** Local execution via the Antigravity CLI or equivalent local agentic harness.
*   **Mechanism:** The local daemon unzips the payload and runs the deterministic `010` evaluators and verifiers directly against the user's filesystem.
*   **Boundary:** Operations are restricted to mathematical checking unless formally elevated by a Sovereign Apply Permit.

## 2. Runtime Handoff

The zip bundle acts as an agnostic transport layer. Upon extraction, the contained `README.md` and `FIRST_CONTACT_TRIAL.md` route the operator to their chosen execution pathway. 

This architecture guarantees that the mathematical proofs and SysML models can be mechanically evaluated independent of the specific agentic framework, preserving the integrity of the categorical blueprint.
